#include <stdio.h>

/**
 * main - Entry point
 * 
 * create a string of characters
 * 
 * Return: 0 (Success)
*/

int main(void)
{
    char username[] = "fsociety123";

    printf("Your username is: %s\n", username);
    return (0);
}