#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * 
 * Perform addition between numbers
 * 
 * Return: 0 (Success)
*/

int main(void)
{
    int a = 10;
    double b = 20.0;
    int c = 5;
    int sum;

    sum = a + c;

    printf("The sum of a and c is: %d\n", sum);
    
    return (0);
}